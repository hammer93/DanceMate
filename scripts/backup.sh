#!/usr/bin/env bash
set -euo pipefail

# DanceMate - backup
#
# v0.74에서 다음을 backup/ 아래로 저장할 예정이다.
#   - PostgreSQL dump
#   - runtime persistent data
#   - Recommendation Memory / Runtime Outcome
#
# 현재 단계에서는 backup할 runtime data가 존재하지 않는다.

echo "DanceMate v0.74 runtime is not installed yet."
exit 0
