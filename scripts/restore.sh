#!/usr/bin/env bash
set -euo pipefail

# DanceMate - restore
#
# v0.74에서 backup/ 의 아카이브로부터 PostgreSQL 및 runtime data를 복구할 예정이다.
# 복구는 파괴적 동작이므로 대상 확인 및 명시적 확인 절차를 반드시 포함한다.
#
# 현재 단계에서는 복구할 runtime이 존재하지 않으며, 어떠한 삭제도 수행하지 않는다.

echo "DanceMate v0.74 runtime is not installed yet."
exit 0
