[pytest]
# Product repository tests (runtime, scheduler, deployment configuration).
# The Information Engine has its own suite: run it from engine/ where
# engine/pytest.ini applies.
pythonpath = .
testpaths = tests
markers =
    postgres: needs a reachable PostgreSQL; skipped when there is none
